[project]
name = "lcu"
version = "0.0.1"
description = "Auto Match Acceptor for Riot Games' League of Legends"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lcu-driver>=3.0.2",
    "loguru>=0.7.3",
]
